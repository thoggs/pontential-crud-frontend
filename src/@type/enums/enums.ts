export enum RequestType {
  GET = 'GET',
  PUT = 'PUT',
  POST = 'POST',
  DELETE = 'DELETE'
}

export enum Genre {
  M = 'M',
  F = 'F',
  O = 'O'
}

export enum RequestStatus {
  SUCCESS,
  ERROR,
  INITIAL_VALUE
}

export enum IsSignedStatus {
  TRUE,
  FALSE,
}
